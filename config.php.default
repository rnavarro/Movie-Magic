<?php

/*
 * All of our necessary config variables go in here!
 */

$source_dir = "";
$dest_dir = "";

$movie_extensions = array('mkv', 'wmv', 'avi');

// Custom Stuff for the way I like to organize my asian movies
$en_asian = TRUE; // Enable asian movie seperation?
$dest_dir_asian = "";
$asian_languages = array(
        'Mandarin', 'Cantonese', 'Japanese',
        'Thai', 'Korean', 'Filipino'
);

$tmdb_api_key = "";

?>
